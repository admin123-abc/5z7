# ============================================================
#  requirements.txt — 项目依赖
# ============================================================

# ── 核心 GUI（必须安装）────────────────────────────────────────
pygame>=2.5.0          # 图形界面渲染引擎（音效/动画/鼠标交互）

# ── 可选：RL AI 支持 ─────────────────────────────────────────
# PyTorch（在 agents/rl_ai.py 中实现神经网络推理时需要）
# torch>=2.0.0
# numpy>=1.24.0

# ── 开发工具（可选）─────────────────────────────────────────
# pytest>=7.0.0
# black>=23.0.0
